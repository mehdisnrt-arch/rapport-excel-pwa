name: Release Pages deployment lock

on:
  push:
    branches: ["main"]
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Release old Pages workflow lock
        run: echo "Pages deployment lock released"
